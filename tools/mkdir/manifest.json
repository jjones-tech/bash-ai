{
  "name": "mkdir",
  "description": "Creates a new directory at the specified path. Can create nested directories.",
  "parameters": {
    "type": "object",
    "properties": {
      "path": {
        "type": "string",
        "description": "Path of the directory to create"
      }
    },
    "required": ["path"]
  }
}
